<?php
require dirname(__DIR__).'/includes/bootstrap.php';require ROOT_PATH.'/includes/services.php';require_borrower();$errors=[];$u=current_user();
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$ids=(array)($_POST['device']??[]);$reason=post('ser_reason');try{create_student_request((int)$u['m_id'],$ids,$reason);flash('success','ส่งคำขอยืมเรียบร้อย กรุณารอเจ้าหน้าที่อนุมัติ');redirect('student/requests.php');}catch(Throwable $e){$errors[]=$e->getMessage();}}
$rows=fetch_all("SELECT d.*,t.t_name,s.s_name FROM tbl_devices d JOIN tbl_devices_type t ON t.t_id=d.ref_t_id JOIN tbl_devices_status s ON s.s_id=d.ref_s_id WHERE d.ref_s_id=1 AND NOT EXISTS(SELECT 1 FROM tbl_devices_service ps WHERE ps.ref_d_id=d.d_id AND ps.ser_status='pending') ORDER BY d.d_name,d.d_id");render_header('ยืมครุภัณฑ์','borrow');
?>
<div class="page-intro"><div><h2>เลือกครุภัณฑ์ที่ต้องการยืม</h2><p>สามารถเลือกหลายรายการในคำขอเดียว รูปที่แสดงอ่านจาก <b>uploads/devices/</b> โดยตรง</p></div><span class="badge badge-success"><?=count($rows)?> พร้อมให้ยืม</span></div><?php if($errors):?><div class="inline-alert danger"><b>!</b><span><?=implode('<br>',array_map('e',$errors))?></span></div><?php endif;?>
<form method="post"><?=csrf_field()?><div class="card"><div class="card-header"><div><h3>ครุภัณฑ์ที่พร้อมใช้งาน</h3><p>เลือกอย่างน้อย 1 รายการ</p></div></div><div class="card-body"><div class="form-group"><div class="table-search" style="max-width:420px;margin-bottom:14px"><input type="search" placeholder="ค้นหาชื่อ รหัส หรือประเภท…" data-table-search="#borrowTable"></div><div class="table-wrap"><table class="data-table" id="borrowTable"><thead><tr><th style="width:55px">เลือก</th><th>รูป / อุปกรณ์</th><th>ประเภท</th><th>รายละเอียด</th></tr></thead><tbody><?php if(!$rows):?><tr data-empty><td colspan="4"><div class="empty-state"><div class="empty-icon">▣</div><h3>ไม่มีครุภัณฑ์ที่พร้อมให้ยืม</h3><p>กรุณากลับมาตรวจสอบอีกครั้งภายหลัง</p></div></td></tr><?php else:foreach($rows as $r):$img=device_image($r);?><tr><td><input type="checkbox" name="device[]" value="<?=e($r['d_id'])?>" style="width:18px;height:18px;accent-color:#2563eb"></td><td><div class="device-cell"><?php if($img):?><img class="device-thumb" src="<?=e($img)?>" alt="<?=e($r['d_name'])?>"><?php else:?><span class="device-placeholder">ET</span><?php endif;?><span><b class="cell-title"><?=e($r['d_name'])?></b><small class="cell-sub">รหัส <?=e($r['d_id'])?></small></span></div></td><td><span class="badge badge-primary"><?=e($r['t_name'])?></span></td><td><?=e($r['d_detail']?:'ไม่มีรายละเอียด')?></td></tr><?php endforeach;endif;?></tbody></table></div></div><div class="divider"></div><div class="form-group"><label>วัตถุประสงค์การยืม <span class="required">*</span></label><textarea class="form-control" name="ser_reason" maxlength="255" required placeholder="เช่น ใช้นำเสนอโปรเจกต์ในชั้นเรียน"><?=e($_POST['ser_reason']??'')?></textarea><small class="form-help">ระบุให้ชัดเจนเพื่อช่วยให้เจ้าหน้าที่ตรวจสอบคำขอได้รวดเร็ว</small></div></div><div class="card-footer"><div class="form-actions"><a class="btn btn-soft" href="<?=e(url('student/dashboard.php'))?>">ยกเลิก</a><button class="btn btn-primary" type="submit" <?=$rows?'':'disabled'?>>ส่งคำขอยืม</button></div></div></div></form><?php render_footer();?>
